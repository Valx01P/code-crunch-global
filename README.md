# Code Crunch Global — redirect only

This repository no longer holds any application source. It exists only
to redirect visitors to the live site:

**https://codecrunchglobal.vercel.app**

Everything here is a static redirect stub: an edge redirect in
`vercel.json`, plus a meta refresh and a script redirect in
`index.html` for hosts that do not read `vercel.json`.
